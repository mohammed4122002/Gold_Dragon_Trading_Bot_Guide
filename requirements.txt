# ═══════════════════════════════════════════════════════════════
#  Gold Dragon Bot — التبعيات الأساسية
#  pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════

pandas>=2.0,<3.0
numpy>=1.24
PyYAML>=6.0
requests>=2.31        # إشعارات Telegram (HTTP متزامن)
schedule>=1.2         # جدولة اختيارية للمهام الدورية

# ── الوسيط الحقيقي ─────────────────────────────────────────────
# MetaTrader5 متاحة على Windows فقط. كل ما عداها يعمل على أي نظام،
# والتشغيل التجريبي (dry-run) والـ Backtest لا يحتاجانها إطلاقاً.
# ثبّتها يدوياً على خادم التداول:
#   pip install MetaTrader5>=5.0.45
